{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "chrome",
      "request": "launch",
      "webRoot": "${workspaceFolder}",
      "url": "http://localhost:3000/",
      "preLaunchTask": "Run Dev"
    }
  ]
}
